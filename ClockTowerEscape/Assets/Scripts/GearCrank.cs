using UnityEngine;

public class GearCrank : MonoBehaviour
{
    // This script is no longer used — gear puzzle solves automatically when chain connects.
    // Kept to avoid missing script errors on existing scene objects.
}
