using UnityEngine;

// Small script here just to hold one variable the type it is
// This is put on each loose gear to identify it, will eventualyl add more types here later
// gearType has to match the GearSlot's requiredGearType to fit
public class GearInfo : MonoBehaviour
{
    public string gearType = "small";
}
